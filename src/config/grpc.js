// Config for grpc client

export const STELLAR = 'stellar'

export const EVRYNET = 'evrynet'

export const TRANSFER = 'transfer'

export const DEFAULT_HOST = 'localhost:8080'

export default {
    STELLAR,
    EVRYNET,
    TRANSFER,
    DEFAULT_HOST
}